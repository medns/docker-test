name: 'block'

on:
  pull_request_target:
    types:
      - auto_merge_enabled

jobs:
  block:
    runs-on: ubuntu-latest
    steps:  
    - run: |
        echo "${{ github.event.reason }}"
        echo "${{ github.event.sender.login }}"
        exit 0
