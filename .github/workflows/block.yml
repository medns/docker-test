name: 'block'

on:
  workflow_run:
    workflows: 
      - \[gh\] pull request checks approval
    types:
      - completed

jobs:
  block:
    runs-on: ubuntu-latest
    steps:  
    - run: |
        exit 0
